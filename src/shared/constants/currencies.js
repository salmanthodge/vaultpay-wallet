/** Supported wallet currencies (ISO 4217). */
export const currencies = Object.freeze(['USD', 'EUR', 'GBP', 'INR', 'AED']);

/** Wallet status values. */
export const walletStatus = Object.freeze({
  ACTIVE: 'ACTIVE',
  FROZEN: 'FROZEN',
  CLOSED: 'CLOSED',
});
